{
  "name": "prompt-print-demand-mern",
  "version": "1.0.0",
  "description": "Print-on-Demand E-Commerce Website (HTML/TS/Tailwind)",
  "scripts": {
    "dev": "concurrently \"npx tailwindcss -i ./src/style.css -o ./dist/style.css --watch\" \"live-server\"",
    "build": "tsc && npx tailwindcss -i ./src/style.css -o ./dist/style.css --minify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "live-server": "^1.2.2",
    "tailwindcss": "^3.4.1",
    "typescript": "^5.3.3"
  }
}
