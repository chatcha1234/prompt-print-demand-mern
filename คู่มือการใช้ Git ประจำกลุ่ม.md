# ⭐ คู่มือการใช้ Git ประจำกลุ่ม

เอกสารนี้สรุปแนวทางการทำงานร่วมกันด้วย Git สำหรับโปรเจกต์กลุ่ม เพื่อให้การจัดการโค้ดเป็นระเบียบและลดข้อขัดแย้ง (Conflicts)

---

## 🌳 โครงสร้าง Branch

| Branch | รายละเอียด |
| :--- | :--- |
| **`main`** | โค้ดที่สมบูรณ์ที่สุด (Production-ready) **ห้ามแก้ไขโดยตรง** |
| **`develop`** | โค้ดรวมงานทั้งหมดที่กำลังพัฒนา แต่ยังไม่เสร็จ 100% |
| **`feature/*`** | พื้นที่สำหรับเขียนงานจริงของแต่ละคน (เช่น `feature/login-page`) |

> [!IMPORTANT]
> สมาชิกทุกคนต้องทำงานใน **`feature branch`** เท่านั้น ห้าม push เข้า `main` หรือ `develop` โดยตรง

---

## 👑 ฝั่ง Repo Master (RPM)

### หน้าที่หลัก
1. ดูแล Repository หลัก
2. สร้าง Branch `main` และ `develop`
3. ตรวจสอบและ Merge Pull Request (PR)
4. อัปเดต Config และโครงสร้างโปรเจกต์
5. แจ้งทีมเมื่อมีการอัปเดตที่สำคัญใน `develop`

### Workflow ของ RPM

#### 1. สร้าง Repo หลัก
*   สร้าง `main` และ `develop`
*   ตั้งค่า **Branch Protection**:
    *   `main`: ห้าม push โดยตรง
    *   `develop`: ต้องแก้ไขผ่าน PR เท่านั้น

#### 2. แก้ไข Config / โครงสร้าง
เมื่อต้องการแก้ไขไฟล์ระบบ ให้ทำผ่าน `develop`:
```bash
git checkout develop
git pull
# แก้ไขไฟล์...
git add .
git commit -m "Update config"
git push origin develop
```

#### 3. รวมงานของสมาชิก
*   รอรับ PR จากสมาชิก
*   ตรวจสอบโค้ด (Code Review)
*   Merge เข้า `develop` (แก้ไข Conflict ถ้ามี)

#### 4. ปล่อย Release
เมื่อโปรเจกต์พร้อมใช้งานจริง:
```bash
git checkout main
git merge develop
git push origin main
```

---

## 👥 ฝั่งสมาชิกทีม (Team Member)

### หน้าที่หลัก
1. Fork Repo หลัก
2. Clone Repo ของตัวเอง
3. ดึงงานจาก Upstream (Repo Master)
4. สร้าง Branch ของตัวเองเพื่อทำงาน
5. ส่ง PR กลับไปยัง `develop` ของ Repo Master

### Workflow ของสมาชิกทีม ss

#### 1. Setup ครั้งแรก
1. **Fork** Repo จาก GitHub ของ RPM
2. **Clone** Repo ของตัวเองลงเครื่อง:
   ```bash
   git clone https://github.com/YOUR_USER/project.git
   cd project
   ```
3. **เพิ่ม Remote Upstream** (เพื่อดึงโค้ดล่าสุดจากทีม):
   ```bash
   git remote add upstream https://github.com/RPM_USER/project.git
   ```
   *เช็คความถูกต้องด้วย `git remote -v` ต้องเห็นทั้ง `origin` (ของเรา) และ `upstream` (ของทีม)*

#### 2. เริ่มงานใหม่ทุกครั้ง (Sync Upstream)
ก่อนเริ่มงาน ต้องดึงโค้ดล่าสุดจากทีมเสมอ:
```bash
git fetch upstream
git checkout develop
git merge upstream/develop
git push origin develop
```

#### 3. สร้าง Branch เพื่อทำงาน
ตั้งชื่อให้สื่อความหมาย เช่น `feature/ชื่อ-งาน`:
```bash
git checkout -b feature/golf-index
```

#### 4. บันทึกงาน (Commit & Push)
เมื่อทำงานเสร็จ:
```bash
git add .
git commit -m "Add UI for index page"
git push -u origin feature/golf-index
```

#### 5. ส่งงาน (Pull Request)
1. ไปที่ GitHub Repo ของตัวเอง
2. กด **Compare & pull request**
3. ตั้งค่า PR:
   *   **Base repository**: `RPM/project` (branch `develop`)
   *   **Head repository**: `YOUR/project` (branch `feature/...`)
4. รอ RPM ตรวจสอบและ Merge

#### 6. อัปเดตงานระหว่างวัน
ถ้ามีการอัปเดตใน `develop` และต้องการนำมาใส่ใน feature branch ของเรา:
```bash
# 1. อัปเดต develop ในเครื่องก่อน
git fetch upstream
git checkout develop
git merge upstream/develop

# 2. กลับไปที่ feature branch แล้ว merge develop เข้ามา
git checkout feature/golf-index
git merge develop
# แก้ไข conflict (ถ้ามี)
git push
```

---

## 📌 สรุป Checklist

### Repo Master (RPM)
- [ ] อัปเดต `develop` เป็นหลัก
- [ ] Merge PR จากสมาชิก
- [ ] ดูแลความเรียบร้อยของ Repo

### Team Member
- [ ] **Fork** Repo
- [ ] **Clone** Repo ของตัวเอง
- [ ] **Set Upstream** (`git remote add upstream ...`)
- [ ] **Sync** ก่อนเริ่มงาน (`git fetch upstream` -> `merge`)
- [ ] **Branch** (`git checkout -b feature/...`)
- [ ] **Work** & **Commit**
- [ ] **Push** ไปที่ Origin
- [ ] **PR** เข้า `develop` ของ RPM
